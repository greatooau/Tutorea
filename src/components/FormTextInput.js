import { StyleSheet, View, TextInput } from 'react-native';


const FormTextInput = () => {
    return(
        <View>
            <TextInput placeholder='When haces tus momos en react native'></TextInput>
        </View>
    );
};

export default FormTextInput;